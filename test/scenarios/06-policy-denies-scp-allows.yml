# Test: Policy doesn't grant S3, but SCP would allow it
# Expected: implicitDeny (policy must grant, SCP only limits)

policy_json: "../policies/allow-ec2.json"

scp_paths:
  - "../scp/permissive.json"

tests:
  - action: "s3:GetObject"
    resource: "arn:aws:s3:::test-bucket/*"
    expect: "implicitDeny"

  - action: "s3:PutObject"
    resource: "arn:aws:s3:::test-bucket/*"
    expect: "implicitDeny"
