version: 1

env:
  - GO111MODULE=on
  - CGO_ENABLED=0

flags:
  - -trimpath
  - -tags=netgo

goos: linux
goarch: amd64

binary: politest-{{ .Os }}-{{ .Arch }}

ldflags:
  - "{{ .Env.GOFLAGS_LDFLAGS }}"
